   name = "srt-persian-translator"
   main = "worker.js"
   compatibility_date = "2024-05-01"
   
   # Optional: Add a custom domain if you have one, otherwise it will use a .workers.dev subdomain
   # routes = [
   #   { pattern = "translate.yourdomain.com", custom_domain = true }
   # ]